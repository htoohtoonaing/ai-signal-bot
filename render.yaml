services:
  - type: web
    name: ai-signal-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn ai_signal_api:app --host 0.0.0.0 --port 10000
    plan: free
